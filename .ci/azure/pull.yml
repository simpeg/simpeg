jobs:
  - job:
    pool:
      vmImage: 'ubuntu-latest'
    displayName: Download artifacts
    steps:
      - checkout: none

      - download: current
        patterns: "file_*"

      - bash: ls -la
        displayName: "List files"
